# Ex2
This program contains definitions for the classes Cell and SpreadSheet.

Each cell can contain a string value, which might represent text, a number, or a mathematical expression.
Mathematical expressions can be solved using the cell's ComputeForm method.

A spreadsheet contains a 2D array of cells and methods to set and get the value of each cell.
